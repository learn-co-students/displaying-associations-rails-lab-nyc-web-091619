<h1>Artists</h1>

<% @artists.each do |artist| %>
   <h2> Artist Name: <%= artist.name %> </h2>
   <h3><%= pluralize(artist.song_count, 'song') %></h3>
    <% artist.songs.each do |song| %>
    <h3> <%= link_to song.title, artist_path(song.artist_id) %> </h3>
      <% end %>
<% end %>